# 윈도우에서 ANSI 로 저장된 파일을 UTF8 로 변환하는 프로그램

### 소개

* 윈도우에서 ANSI 로 저장된 파일을 UTF8 로 변환하는 프로그램
* 입력된 폴더에 포함된 모든 파일들을 UTF8 로 변환하여서 저장하는 프로그램

### 라이선스

* 본 프로젝트는 GPLv3 라이선스입니다. 

### 사용법

```
dos> AnsiToUtf8 {folder full path} 
```



